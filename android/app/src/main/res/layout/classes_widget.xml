<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/widget_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/widget_glass_background"
    android:orientation="vertical"
    android:padding="16dp"
    android:paddingTop="14dp">

    <!-- Header Row -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center_vertical"
        android:orientation="horizontal"
        android:layout_marginBottom="8dp">

        <!-- Teal liquid dot -->
        <FrameLayout
            android:layout_width="7dp"
            android:layout_height="7dp"
            android:background="@drawable/widget_dot_teal"
            android:layout_marginRight="7dp" />

        <TextView
            android:id="@+id/widget_title"
            android:layout_width="0dp"
            android:layout_weight="1"
            android:layout_height="wrap_content"
            android:text="TODAY'S CLASSES"
            android:textColor="#E02DD4BF"
            android:textSize="10sp"
            android:textStyle="bold"
            android:letterSpacing="0.14" />

        <!-- Date badge -->
        <TextView
            android:id="@+id/widget_date_label"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textColor="#99FFFFFF"
            android:textSize="10sp"
            android:letterSpacing="0.04" />
    </LinearLayout>

    <!-- Divider (FrameLayout required by RemoteViews — bare View not allowed) -->
    <FrameLayout
        android:id="@+id/widget_divider"
        android:layout_width="match_parent"
        android:layout_height="1dp"
        android:background="#40FFFFFF"
        android:layout_marginBottom="7dp" />

    <!-- Empty State -->
    <TextView
        android:id="@+id/empty_state"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:gravity="left|center_vertical"
        android:text="You're all clear! ✨"
        android:textColor="#99FFFFFF"
        android:textSize="13sp"
        android:visibility="gone" />

    <!-- Scrollable ListView -->
    <ListView
        android:id="@+id/classes_list_view"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:divider="@null"
        android:dividerHeight="0dp"
        android:scrollbars="none"
        android:visibility="visible" />

</LinearLayout>
